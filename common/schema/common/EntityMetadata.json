{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://cyoda.com/cloud/event/search/EntityMetadata.json",
  "title": "EntityMetadata",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "description": "ID of the entity.",
      "format": "uuid"
    },
    "modelKey": {
      "$ref": "../common/ModelSpec.json",
      "description": "Model of the entity."
    },
    "state": {
      "type": "string",
      "description": "The state of the entity."
    },
    "creationDate": {
      "type": "string",
      "format": "date-time",
      "description": "The creation date of the entity."
    },
    "lastUpdateTime": {
      "type": "string",
      "format": "date-time",
      "description": "The last time the entity was updated. Equals the creation date if the entity has not been updated."
    },
    "pointInTime": {
      "type": "string",
      "format": "date-time",
      "description": "Optional value for the as-at point-in-time for which the entity was retrieved."
    },
    "transitionForLatestSave": {
      "type": "string",
      "description": "The transition applied of the entity when last saved."
    }
  },
  "required": [
    "id",
    "state",
    "creationDate",
    "lastUpdateTime"
  ]
}